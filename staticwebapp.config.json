{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/api/*"]
  },
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://profiseebespokedbikesapi.azurewebsites.net/api/:*"
    }
  ]
}